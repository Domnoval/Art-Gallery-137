# Artist Gallery Uploader - Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=3000

# Base directory for local gallery storage
GALLERY_DIR=./local_gallery_db

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ===========================================
# API VAULT CONFIGURATION
# ===========================================
# The app uses a local API Vault to manage keys securely
# Set to 'false' to use direct API keys instead of vault
USE_VAULT=true

# Vault proxy URL (default: http://localhost:9999/proxy/anthropic/)
VAULT_URL=http://localhost:9999/proxy/anthropic/

# ===========================================
# DIRECT API KEYS (Only if USE_VAULT=false)
# ===========================================
# If not using the vault, uncomment and fill these:
# ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
# WIX_API_KEY=your-wix-api-key-here
# WIX_SITE_ID=your-wix-site-id-here

# ===========================================
# NOTE: API Keys should go in tools/api-vault/.env
# ===========================================
# See tools/api-vault/.env.example for the full list
# The vault centralizes all sensitive credentials
